import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    tamagochiContainer : {
        borderColor : "red",
        borderBottomWidth : 5,
        borderTopWidth : 5,
        borderLeftWidth : 5,
        borderRightWidth : 5,
        height : "100%",
        width : "100%",
        padding : -20
    }
})